# Base URL of the backend API, including the /api/v1 prefix.
# No hardcoded default belongs in code — app/api-client.ts falls back to
# this exact value in dev if the variable isn't set.
VITE_API_BASE_URL=http://localhost:8000/api/v1
